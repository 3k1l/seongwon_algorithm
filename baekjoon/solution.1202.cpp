#include <iostream>
#include <vector>
#include <algorithm>

using namespace std;

int N;
int K;
vector<pair<int, int> > gem;
vector<int> c;
int main()
{
    cin >> N >> K;

    for (int i = 0; i < N; i++)
    {
        int m, v;
        cin >> m >> v;
        gem.push_back(pair<int, int>(m, v));
    }
    for (int j = 0; j < K; j++)
    {
        cin >> 
    }
}